/**
 * Тактильный отклик отключён: на Android вибрация на каждое нажатие мешала.
 * Вызовы оставлены, чтобы при желании вернуть отклик в одном месте.
 */
const none = () => {}

export const haptics = {
  /** Нажатие кнопки, выбор в списке. */
  tap: none,
  /** Успешное действие: операция создана, смена назначена. */
  success: none,
  /** Предупреждение: конфликт в графике, незакрытый месяц. */
  warn: none,
  /** Ошибка: не прошла валидация или запрос. */
  error: none,
}
